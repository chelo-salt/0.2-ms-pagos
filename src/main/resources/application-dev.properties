spring.application.name=ms-pagos

server.port=8088

# 🗄️ CONFIGURACIÓN DE BASE DE DATOS
spring.datasource.url=jdbc:mysql://localhost:3318/bd_pagos
spring.datasource.username=root
spring.datasource.password=system

# ☕ CONFIGURACIÓN DE JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 🪵 CONFIGURACIÓN DE FLYWAY
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-mispring.application.name=ms-pagos

server.port=8088

# 🗄️ CONFIGURACIÓN DE BASE DE DATOS
spring.datasource.url=jdbc:mysql://localhost:3318/bd_pagos
spring.datasource.username=root
spring.datasource.password=system

# ☕ CONFIGURACIÓN DE JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 🪵 CONFIGURACIÓN DE FLYWAY
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.clean-disabled=false

# 🔐 SEGURIDAD
jwt.secret=ClaveUltraSecretaEInviolableParaLaMunicipalidad2026!

# 🌐 REDIRECCIÓN AL API GATEWAY (RESPALDO DE PERFIL)
springdoc.openapi.servers[0].url=http://localhost:8080
springdoc.openapi.servers[0].description=API Gateway (Entorno Local)grate=true
spring.flyway.clean-disabled=false

# 🔐 SEGURIDAD
jwt.secret=ClaveUltraSecretaEInviolableParaLaMunicipalidad2026!

# 🚀 REDIRECCIÓN DE SWAGGER AL API GATEWAY
# Configura el puerto 8080 del Gateway como el servidor de pruebas por defecto
springdoc.openapi.servers[0].url=http://localhost:8080
springdoc.openapi.servers[0].description=API Gateway (Entorno Local)